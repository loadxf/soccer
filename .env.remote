# Soccer Prediction System - Remote Server Configuration

# Remote Server Configuration
REMOTE_API_HOST=103.163.186.204
API_PORT=8000

# API Version
API_VERSION=v1

# Connection Settings
REQUEST_TIMEOUT=10
MAX_RETRIES=3

# Application Settings
APP_ENV=production
DEBUG=False

# Note: This file should be loaded when connecting to the remote server
# You can source this file with: 
# export $(grep -v '^#' .env.remote | xargs)

# Host listening settings - bind to all interfaces
HOST=0.0.0.0
PORT=8000

# Database Configuration 
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=soccer_prediction
DB_USER=postgres
DB_PASSWORD=your_secure_password

# Frontend Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Logging
LOG_LEVEL=INFO
LOG_FILE=app.log

# CORS settings - enable all origins for testing
# Update with specific domains in production
CORS_ALLOW_ALL=True

# Security
# Set to True in production
REQUIRE_API_KEY=False 
API_KEY=your_api_key_here

# System paths
MODEL_CACHE_DIR=./model_cache
DATA_DIR=./data
UPLOAD_DIR=./uploads 