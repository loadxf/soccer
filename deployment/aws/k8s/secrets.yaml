# This is a template file. DO NOT store actual secrets in version control.
# Replace the placeholder values with actual secrets before applying.
apiVersion: v1
kind: Secret
metadata:
  name: soccer-prediction-secrets
type: Opaque
stringData:
  database_url: "postgresql://username:password@rds-endpoint:5432/soccer_prediction"
  api_secret_key: "your-api-secret-key-here"
  jwt_secret_key: "your-jwt-secret-key-here" 