# Soccer Prediction System Frontend Environment Variables

# API URL for Docker environment (using container service name)
REACT_APP_API_URL=http://app:8000

# Set to 'docker' for Docker-specific behavior
REACT_APP_ENVIRONMENT=docker

# Enable service worker for offline capabilities
REACT_APP_ENABLE_SERVICE_WORKER=true

# Other settings
REACT_APP_VERSION=$npm_package_version
REACT_APP_NAME=$npm_package_name 